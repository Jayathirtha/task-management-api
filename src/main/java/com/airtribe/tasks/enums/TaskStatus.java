package com.airtribe.tasks.enums;

/**
 * Single Responsibility Principle (SRP): This enum has one responsibility:
 * to define the possible states of a task.
 */
public enum TaskStatus {
    PENDING,
    IN_PROGRESS,
    COMPLETED
}